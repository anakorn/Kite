SELECT 
all_members_count, 
attending_count, 
can_invite_friends,
creator,
declined_count,
description,
eid,
end_time,
has_profile_pic,
hide_guest_list,
host,
is_date_only,
location,
name,
not_replied_count,
pic,
pic_big,
pic_small,
pic_square,
privacy,
start_time,
ticket_uri,
timezone,
unsure_count,
update_time,
venue 
FROM event 
WHERE eid IN (SELECT eid FROM event_member WHERE uid = me())